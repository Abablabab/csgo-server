sv_lan 0 // Enables passwords, VAC, requires connection to Steam

ip 0.0.0.0
hostname "Noobville"

// replaced by secrets.cfg
rcon_password SECRET
sv_password SECRET
tv_password SECRET
sv_setsteamaccount SECRET

tv_enable 0 // Not the same as broadcasting, just enables the system
tv_autorecord 0 // write demos to disk

exec banned_user.cfg
exec banned_ip.cfg

exec secrets.cfg
